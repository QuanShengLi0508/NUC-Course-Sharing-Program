`timescale 1ns/1ns
`include "adder4.v"

module adder_tb;
  reg [3:0] a, b; // ??ina?inb
  reg cin;
  wire [3:0] sum;
  wire cout;
  integer i;

  // ???adder4??
  adder4 adder_inst(cout, sum, a, b, cin);

  // ??????????
  initial begin
    // ????????????
    cin = 0; // ???????0
    $monitor($time, " | a = %d | b = %d | cin = %b | sum = %d | cout = %b", a, b, cin, sum, cout);

    // ????a?b?????
    for (i = 0; i < 9; i = i + 1) begin
      a = i + 1; // a?1?9
      b = i;     // b?0?8
      #10;       // ??10ns???????
    end

    // ???????
    #10 $stop;
  end
endmodule
